query getConsignmentSeriesAndBody($consignmentId: UUID!) {
    getConsignment(consignmentid: $consignmentId) {
        series {
            code
        },
        transferringBody {
            name
        }
    }
}