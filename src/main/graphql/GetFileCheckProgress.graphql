query getFileCheckProgress($consignmentId: UUID!) {
    getConsignment(consignmentid: $consignmentId) {
        totalFiles
        fileCheckProgress {
            antivirusProgress {
                processedFiles
            }
        }
    }
}
