query getConsignmentStatus($consignmentId: UUID!) {
    getConsignment(consignmentid: $consignmentId) {
        series {
            code
        },
        currentStatus {
            upload
        }
    }
}
