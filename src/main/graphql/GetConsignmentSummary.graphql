query getConsignmentSummary($consignmentId: UUID!) {
    getConsignment(consignmentid: $consignmentId) {
        series {
            code
        },
        transferringBody {
            name
        }
        totalFiles
    }
}
